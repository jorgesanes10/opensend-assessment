import { LogoutButton } from '../components/LogoutButton';

export const Admin = () => {
  return (
    <main>
      <h1 className="mb-2 text-3xl text-gray-900">Admin page</h1>
      <LogoutButton />
    </main>
  );
};
